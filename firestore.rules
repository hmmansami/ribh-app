rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is via Admin SDK (Cloud Functions), which bypasses rules entirely.
    // Lock down all client-side access — no direct reads or writes from browsers.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
